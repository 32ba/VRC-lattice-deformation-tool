# Legacy folder identity

Weight transfer implementation is maintained in `Editor/MeshDeformer/WeightTransfer`.

This folder retains its original Unity GUID for package upgrade compatibility. Keeping this note also ensures UnityPackage import preserves the folder instead of discarding an empty directory.
